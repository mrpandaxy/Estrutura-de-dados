#include<stdio.h>
#include<stdlib.h>
#include<string.h>
#include"hash.h"

int main(int arg, char* argv){

char* tabela[10];

tabela[0]="mel";
tabela[1]="futebol";

printf("%s\n",tabela[0]);
printf("%s\n",tabela[1]);

exit(0);

}